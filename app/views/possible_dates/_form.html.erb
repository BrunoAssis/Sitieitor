<%= form_for(@possible_date) do |f| %>
  <% if @possible_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@possible_date.errors.count, "error") %> prohibited this possible_date from being saved:</h2>

      <ul>
      <% @possible_date.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label "event_id", "Event" %>
    <%= collection_select(:event, :id, @events, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :date_from %><br />
    <%= f.datetime_select :date_from %>
  </div>
  <div class="field">
    <%= f.label :date_to %><br />
    <%= f.datetime_select :date_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
